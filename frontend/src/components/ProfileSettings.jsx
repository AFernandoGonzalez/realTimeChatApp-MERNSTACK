import './ProfileSettings.css';

export const ProfileSettings = () => {

    return (
        <div>
            <a href="/logout" className="btn btn-primary">
                Log out
            </a>
        </div>
    )

}